os: windows

install:
  - choco install -y conan

matrix:
    include:        
        - language: cpp
          script:
            - mkdir build
            - cd build
            - cmake ..
            - cmake --build . --config Release