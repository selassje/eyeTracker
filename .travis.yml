os: windows

install:
  - choco install -y conan
  - `refreshenv`
  - conan -v

matrix:
    include:        
        - language: cpp
          script:
            - mkdir build
            - cd build
            - cmake ..
            - cmake --build . --config Release