os: windows

install:
  - choco install -y conan
  - RefreshEnv.cmd

matrix:
    include:        
        - language: cpp
          script:
            - mkdir build
            - cd build
            - cmake ..
            - cmake --build . --config Release