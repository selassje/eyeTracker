os: windows

install:
  - choco install -y conan
  - RefreshEnv.cmd
  - echo %PATH%
  - conan -v

matrix:
    include:        
        - language: cpp
          script:
            - mkdir build
            - cd build
            - cmake ..
            - cmake --build . --config Release