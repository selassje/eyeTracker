#ifndef CONSTANTS_HPP
#define CONSTANTS_HPP

#include <opencv2/imgproc.hpp>

namespace CVCOLORS {
    const auto RED   = CV_RGB(255, 0, 0);
    const auto GREEN = CV_RGB(0, 255, 0);
    const auto BLUE  = CV_RGB(0, 0, 255);
    const auto BLACK = CV_RGB(0, 0, 0);
    const auto WHITE = CV_RGB(255, 255, 255);
    }

#endif
